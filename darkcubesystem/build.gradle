plugins {
    id 'com.github.johnrengelman.shadow'
    id 'java-library'
}

tasks.shadowJar.archiveClassifier.set('')
tasks.build.dependsOn(tasks.shadowJar)

dependencies {
    api project(path: 'api')
    runtimeOnly project(path: 'bukkit')
    runtimeOnly project(path: 'module')
}
